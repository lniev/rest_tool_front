import { memo, useState, useEffect, FC, useRef } from 'react';
import JsonFormatContainer from '../../container/JsonFormat/JsonFormat.container';

interface IProps {}

const ToolsNav: FC<IProps> = () => {
  useEffect(() => {}, []);

  return (
    <div>
      {/*<div>json转换</div>*/}
      {/*<JsonFormatContainer />*/}
    </div>
  );
};

export default ToolsNav;
